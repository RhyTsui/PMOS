# Component Binding

> Canonical: `docs/architecture/semantic-contract/component-binding-contract.md`

`componentBinding` 是 region 到前端 renderer 的唯一挂载入口。

这份文件保留为便于检索的入口页，核心规则、约束和注册要求以 `component-binding-contract.md` 为准。

