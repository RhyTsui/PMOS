# Agent Runtime Minimal Model

Status: P0.7 architecture refresh.

See `docs/architecture/automation/agent-runtime-minimal-model.md` for the detailed model.

Runtime may project `AgentRuntimeTask` into message metadata, side panels, and disclosure surfaces. It must not replace Request Understanding or Capability Orchestration.
