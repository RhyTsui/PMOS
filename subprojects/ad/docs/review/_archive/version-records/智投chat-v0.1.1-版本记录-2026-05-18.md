# 智投chat v0.1.1 版本记录

## 这次修什么
- 普通对话路径新增能力发现。
- 当用户没有命中固定 agent / skill，但问题明显指向“应用、项目、包、分包、列表、状态”类业务对象时，先搜 MCP tool，再决定是否进入知识库。
- 这次先解决一个明确问题：用户问“获取指间山海的应用列表”时，不再直接掉到知识库和模型解释。

## 行为变化
- 以前：未命中 agent 后，普通对话直接走知识库。
- 现在：先尝试检索 MCP 工具。
- 如果找到工具：
  - 先返回可用工具名称
  - 再检查必要参数
  - 缺参数时明确提示缺口
  - 不再伪造知识库答案
- 如果没找到工具：
  - 才继续走知识库

## 这次的实际结果
- `获取指间山海的应用列表`
  - 已命中真实工具 `巨量引擎 MCP.tools_app_management_android_app_list_v2`
  - 已停止错误的知识库兜底
  - 当前返回缺少参数提示，而不是编造结果

## 回退方式
- 回退 `imported/projects/src/app/api/chat/route.ts`
- 恢复到这次提交前的普通对话路由逻辑

## 验证
- `npm.cmd run build` 通过
- 真实 SSE 请求已验证：
  - 能进入能力发现
  - 不再掉到知识库
  - 能返回可用工具和缺参信息
