# 2026-05-09 v1.1 Frontend Mainline v1

- status: `active`
- date: `2026-05-09`
- owner: `codex`
- topic: `AI埋点平台 v1.1 前端主链重排续跑提示词`
- supersedes: `none`
- voided_by: `none`

## 1. 当前有效判断

当前不要再把工作理解成“在旧页面上补几个点”。

当前有效口径是：

1. 已新增 `v1.1` 需求真源
2. 前端已经切到 `v1.1` 产品形态重排主线
3. 重点不是继续堆静态页面，而是把主链做成：
`版本管理 -> 需求装配 -> 查询验证 -> 验收报告 -> 小闪推送 -> 状态回写`

## 2. 已完成进展

当前已完成：

1. 右上角已接入 `需求版本` 切换，支持 `v1.0 / v1.1`
2. 概览页已加入 `v1.1` 主链总览区
3. 需求装配页已做成四块骨架：
`内置通用模板 / 勾选式补充 / 自定义差异区 / 导入承接区`
4. 查询验证页已加入三块骨架：
`版本查询会话 / 证据回挂 / 快速定位`
5. 验收报告页已加入三块骨架：
`小闪同步简报 / 站内详细报告 / 分发状态回写`
6. 三页已接通 `workflowContext` 跨页联动：
`装配 -> 查询验证/报告`
`查询验证 -> 报告`
`报告 -> 装配来源`
7. 最近多轮改动后，`frontend` 下 `npm run build` 持续通过

## 3. 当前未完成项

当前还没做完的，不要误判为已经闭环：

1. 装配页里的模板、勾选项、差异项还只是静态骨架，没有变成真实状态对象
2. 查询验证页还没有把筛选条件沉淀成真正的“查询会话对象”
3. 报告页还没有把推送状态、来源会话、来源装配项做成完整状态链
4. `docs/product/AI埋点平台-实现真源包索引-v1.md` 仍有编码问题，`v1.1` 还未安全补回索引

## 4. 下次优先顺序

下次续跑时，优先按下面顺序继续：

1. 让需求装配页中的具体模板、勾选项、差异项能作为真实上下文带入查询验证和验收报告
2. 让查询验证页把当前筛选条件和结果沉淀成可回写的查询会话对象
3. 让验收报告页把推送状态、来源会话、来源装配项串成完整状态链

## 5. 建议第一步

下次进入后，先检查这几个文件：

1. [App.jsx](E:\AI\ai-os\subprojects\tracking-acceptance\frontend\src\App.jsx:1)
2. [RequirementsPage.jsx](E:\AI\ai-os\subprojects\tracking-acceptance\frontend\src\pages\RequirementsPage.jsx:1)
3. [RealtimeQueryPage.jsx](E:\AI\ai-os\subprojects\tracking-acceptance\frontend\src\pages\RealtimeQueryPage.jsx:1)
4. [AcceptanceReportsPage.jsx](E:\AI\ai-os\subprojects\tracking-acceptance\frontend\src\pages\AcceptanceReportsPage.jsx:1)

然后优先把“静态骨架上下文”升级成“真实可流转状态”。

## 6. 可直接复制的启动提示词

把下面这段直接发给下次的 Codex / Claude：

```md
继续 `tracking-acceptance` 的 AI埋点平台 `v1.1` 前端主链重排，不要回到旧版“页面补丁”思路。

先按仓库恢复顺序读取：
1. `docs/memory/mcp-context/session-state.json`
2. `npm run cli -- mcp-context events 20`
3. `npm run cli -- mcp-context tasks --status in_progress`
4. `docs/operations/startup-whoami.md`
5. `AGENTS.md`

然后读取这份续跑文档：
`docs/context/session-start-prompts/2026-05-09-v1.1-frontend-mainline-v1.md`

当前已知状态：
1. `v1.1` 需求真源已新增
2. 右上角 `需求版本` 切换已接入
3. 概览页、需求装配页、查询验证页、验收报告页都已按 `v1.1` 做主链骨架化
4. 三页已接通 `workflowContext` 跨页联动
5. `frontend` 最近一次 `npm run build` 已通过

本次续跑不要重复搭骨架，直接继续做真实状态流：
1. 让装配页的模板/勾选/差异项变成真实上下文
2. 让查询验证页沉淀查询会话对象
3. 让报告页把推送状态、来源会话、来源装配项做成完整状态链

补充注意：
`docs/product/AI埋点平台-实现真源包索引-v1.md` 仍有编码问题，当前不要贸然直接改坏它。
```

## 7. 后续更新规则

如果明后天继续推进后，这份提示词不再够用：

1. 新建 `v2`
2. 把本文件状态改成 `superseded`
3. 在新文件里写明 `supersedes: 2026-05-09-v1.1-frontend-mainline-v1.md`

## 8. 作废规则

如果后续产品主线被整体推翻、或不再按 `v1.1` 继续：

1. 保留本文件
2. 将状态改成 `void`
3. 在文首补一行作废原因
4. 如有替代提示词，写明替代文件名
