# 上报结构

- 行数：1
- 列数：1

|uuid varchar(65533) NOT NULL COMMENT "系统生成uuid",   data_day date NOT NULL COMMENT "数据日期，格式yyyy-MM-dd",   app_id varchar(65533) NOT NULL COMMENT "游戏ID",   link_id varchar(65533) NULL COMMENT "链路ID，当次App生命周期内的行为链路ID,link_id+log_id确定唯一事件",   log_id varchar(65533) NULL COMMENT "行为ID，当次行为日志的ID，代表了当前用户行为的顺序",   event_name varchar(65533) NULL COMMENT "事件名称",   event_status varchar(65533) NULL COMMENT "打点事件的成功与失败的标识: 0：成功, 1：失败，2：取消，3：超时",   event_type varchar(65533) NULL COMMENT "事件类型: base：基础; account：账号; ticket：票据; pay：支付; crash：崩溃",   data_report_time bigint(20) NULL COMMENT "数据上报时间，时间戳，单位毫秒",   start_time bigint(20) NULL COMMENT "事件发生时间，时间戳，单位毫秒",   advertis varchar(65533) NULL COMMENT "IDFA(iOS)\谷歌ID",   android_id varchar(65533) NULL COMMENT "安卓ID",   app_package_name varchar(65533) NULL COMMENT "APP包体名称",   app_version varchar(65533) NULL COMMENT "APP版本",   app_version_code varchar(65533) NULL COMMENT "APP版本CODE（细分版本）",   battery_capacity varchar(65533) NULL COMMENT "设备可用电量百分比",   brand varchar(65533) NULL COMMENT "手机品牌",   c_oneid varchar(65533) NULL COMMENT "客户端设备标识",   caid_new varchar(65533) NULL COMMENT "CAID",   caid_new_version varchar(65533) NULL COMMENT "CAID版本",   caid_old varchar(65533) NULL COMMENT "老CAID",   caid_old_version varchar(65533) NULL COMMENT "老CAID版本",   carrier varchar(65533) NULL COMMENT "网络运营商",   channel varchar(65533) NULL COMMENT "包体渠道",   cpsid varchar(65533) NULL COMMENT "渠道分包ID",   device_id varchar(65533) NULL COMMENT "设备ID（客户端或服务端设备唯一标识）",   device_model varchar(65533) NULL COMMENT "设备型号",   idfv varchar(65533) NULL COMMENT "苹果idfv",   imei varchar(65533) NULL COMMENT "IMEI码",   imei2 varchar(65533) NULL COMMENT "IMEI码（双卡）",   installation_time bigint(20) NULL COMMENT "应用安装时间，时间戳，单位：毫秒",   is_simulator varchar(65533) NULL COMMENT "是否模拟器: 0：是, 1：否",   is_jail_broken varchar(65533) NULL COMMENT "是否越狱: 0：是, 1：否",   kern_boot_time bigint(20) NULL COMMENT "设备最近重启时间，时间戳，单位：毫秒",   kern_update_time bigint(20) NULL COMMENT "系统更新时间，时间戳，单位：毫秒",   language varchar(65533) NULL COMMENT "手机系统语言",   login_from varchar(65533) NULL COMMENT "启动方式、来源",   mac_address varchar(65533) NULL COMMENT "MAC地址",   memory_free varchar(65533) NULL COMMENT "设备可用内存容量（B）",   memory_total varchar(65533) NULL COMMENT "设备内存总容量（B）",   mobile_storage_free varchar(65533) NULL COMMENT "设备可用硬盘容量（B）",   mobile_storage_total varchar(65533) NULL COMMENT "设备硬盘总容量（B）",   net_type varchar(65533) NULL COMMENT "网络类型（wifi、移动流量等）",   network_state varchar(65533) NULL COMMENT "网络状态",   oaid varchar(65533) NULL COMMENT "安卓OAID",   operator varchar(65533) NULL COMMENT "运营商编码",   os_type varchar(65533) NULL COMMENT "操作系统",   os_version varchar(65533) NULL COMMENT "操作系统版本",   pixel_metric varchar(65533) NULL COMMENT "屏幕分辨率",   role_id varchar(65533) NULL COMMENT "角色id",   s_oneid varchar(65533) NULL COMMENT "服务端设备标识",   sdk_version varchar(65533) NULL COMMENT "SDK版本",   server varchar(65533) NULL COMMENT "创角区服",   server_zone varchar(65533) NULL COMMENT "游戏服务器时区",   sys_country varchar(65533) NULL COMMENT "手机系统国家(地区)",   timezone varchar(65533) NULL COMMENT "手机系统时区",   token varchar(65533) NULL COMMENT "TOKEN，苹果用来ASA归因",   user_id varchar(65533) NULL COMMENT "SDK账号",   user_type varchar(65533) NULL COMMENT "用户类型: 0：老用户, 1：新用户",   yoka_id varchar(65533) NULL COMMENT "认证系统游卡ID/第三方ID",   is_vpn varchar(65533) NULL COMMENT "是否开了VPN: 0：是, 1：否",   is_root varchar(65533) NULL COMMENT "设备是否已经root: 0：是, 1：否",   ip varchar(65533) NULL COMMENT "IP地址",   device_info varchar(65533) NULL COMMENT "上报的其余设备字段，JSON格式",   event_info varchar(65533) NULL COMMENT "上报的其余事件字段，JSON格式",   receive_time bigint(20) NULL COMMENT "数仓接收时间，时间戳，单位：毫秒"|
|---|

> 注：Markdown 仅保留前 80 行预览；完整数据见同名 CSV。
