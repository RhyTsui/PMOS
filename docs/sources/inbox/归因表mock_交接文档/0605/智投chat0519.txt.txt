
 1、思维链的上下文有点问题，比如刚才问“指间山海今天的消耗多少”，然后告诉我mcp链接失败，我配置好了mcp，再提问“重试
  一下”，竟然把“重试一下”作为关键词检索知识片段。也就是说应该在对话和上下文增加推理思考过程。 其次，没有调用报表
  mcp，第3没有返回正文结果，但是返回了一个json：【 "name": "get_ads_report",
   "parameters": { "account_names": [
        "指间山海"
      ],
      "start_date": "2026-05-19",
      "end_date": "2026-05-19", "metrics": [ "cost"
      ]
    }】他知道tool，但是思维链里没展示，数据也没有取到。


2、展开}应该是收起在这个步骤中，外面展示调用过程和结果即可。 只改动这一个场景，不要动其他的定义。如果改动有关联影
  响要和我确认。
3、结果是{指间山海 2026-05-19 的消耗为 ¥63.44。}，但是BI系统中显示是658.95。需要检查返回数据和结果中的数据是否一
  致。  另外就是tool返回的数据和请求参数一样用代码块组件展示


1.移动端访问时，快捷入口按钮展示成左右2列，去掉描述文字只展示快捷按钮名称。
2. 移动端访问时，右侧侧边栏和左侧侧边栏一样，上层悬浮展示。


点击“验流程”这个快捷入口按钮，用户按模板提问后，展示出该项目的包信息，一行一个包。
思维链是取当前上下文的项目，然后调用智投配置mcp，获取分包列表。（后面会专门提供一个整合了应用的包列表mcp）。如果获取失败，则进行查异常，检查智投配置mcp里包管理tool，确认是否存在QA验收通过的渠道包，如果存在，再去媒体后台mcp检查是否存在审核通过的应用，再去检查默认账户是否有授权过的应用。如果缺信息，直接提示缺口。 也就是说验流程有一个agent，agent调度3个skill，有3个skill，一个skill是执行包管理、分包、联调。一个skill是提供分包信息和状态。一个skill是提供步骤排查。skill绑定了多个mcp，一个是联调mcp、一个是分包mcp、一个媒体配置mcp、一个是智投配置mcp。

方案：
 1. 新增“获取广告包”业务流程
      - 识别用户问题里的“广告包、投放包、可交付包、包体检查、分包、包地址”等意图。
      - 不再走普通问答。
      - 进入固定业务流程：确认项目 -> 查询包列表 -> 检查检测状态 -> 检查审核状态 -> 检查联调状态 -> 输出可交付结
        果。
  2. 新增结果交付规则
      - 成功：正文只展示可交付包列表。
      - 失败：正文只展示最后阻塞项、原因、下一步。
      - 进行中：正文不输出最终结论，只更新过程区。
  3. 新增“获取广告包”过程展示
      - 思维链展示判断过程和推进步骤。
      - 调用与来源展示实际调用的工具、请求、返回。
      - 正文不展示中间过程。
      - 暂不新增复杂页面，先复用现有 chat 结构。
  4. 接入现有 MCP 能力
      - 优先查智投配置 MCP 的包、应用、分包相关工具。
      - 如涉及巨量、腾讯、快手，再根据媒体语义进入媒体 MCP。
      - 不允许因为某个 MCP 找不到就生成假结果。
      - 如果能力缺失，直接返回“当前缺少哪项能力”。
  5. 补充业务歧义处理
      - “应用列表”这类词存在智投应用、媒体应用两个含义。
      - “包”也存在母包、渠道包、分包、媒体后台包。
      - v0.3 要把这类歧义前置识别，需要确认时用简洁按钮，而不是长正文追问。

2.监控 / 定时任务接口
- 系统监控、项目监控、定时任务必须有独立意图路由
- 不能误判成排查、报表或联调
- 需要返回任务状态、执行时间、结果摘要、异常原因
后面再说

v0.3 验流程 inbox：智投Chat_获取广告包验流程_AI交付方案_CodexCLI.md
 1、后台没有看到刚才建的skill mcp agent 或者workflow。
2、打开网址时默认加载了最近一条会话，应该改完默认打开新会话，左侧侧边栏不需要展示一个会话叫“新会话”用户发送后再展示。请参考chatdeepseek。

v0.4 上下文和记忆  inbox：enterprise_ad_chat_context_memory_requirements_spec.md

v0.5 
1.左下角弹窗-我的资产下面增加{自动化}功能  inbox：文件夹名称：自动化设计截图。分析一下如何和定制日周月报，拼表定时跑，选择广告标签聚合汇总分析等自定义指标和维度的需求结合。




1.下面选择没有默认进入上下文 或上下文没有在大模型思维链中看到被引用
2.当查看近7天投放效果，需要在思维链中展示推理和思考，目前大模型好像缺乏思考，调用对应的报表mcp取近7天的指标数据，但是目前返回消息说要指定媒体维度和指标，没指定的时候就展示一个大盘数据呀？而且也不需要操作记录把，不过操作记录是可以获取的，从巨量mcp中找下操作记录tool。对比基准也是去报表mcp取的。
3.分析流程示意图去掉白色背景色块，去掉点击查看大图的指引，去掉流程图小标题文案。
4.行业榜单包括，小游戏榜单（多个小游戏），应用商店榜单（多个应用商店），素材榜单（多个广告平台），榜单上面提供更新摘要的快速解读，通过后台的提示词来优化解读效果。
其中应用商店榜单则按inbox的：photo_2025-09-23_13-28-02.jpg来设计。整体风格统一起来。
再就是素材榜单，这个系统是为了做成https://adxray.dataeye.com/index/home#/Product这样的产品。爬虫等方式获取数据，然后做关联分析建立广告结构化行业数据服务。
5.其中素材还增加一个创意脚本解析，展示创意脚本内容的服务，然后通过创意脚本的相似度匹配，解决相同素材的合并问题，生成创意id，再将相似素材的数据合并，即创意数据。便于创意对比分析，洞察top创意的数据和创意脚本。来源可切换是行业创意还是内部创意。
6.提示模板我已经改为“近30天公司内的创意和行业创意对比分析，然后后台提供一个skill来分析和调优分析”，比如公司内的创意有没有借鉴行业的爆款，借鉴的多不多，公司的创意是否是过去几个月已经处于下行趋势的创意，公司采纳新创意的速度如何，公司有哪些创意是行业还没有的。

1.当前的自动化模块中的日报模板不能满足需求，我提供了业务用的日报模板，在inbox里：日报模板1.docx、游戏项目日报模板.xlsx。主要是看项目总数据、广告量、各媒体流量、各应用类型流量、各媒体各应用类型流量、各团队流量，各团队各应用类型流量，各团队媒体流程，各团队各媒体各应用类型流量，扣除iOS自然量的流量，不扣除iOS自然量的流量。扣除iOS分成比例的流量。
2.当前看数据服务有很多问题，需要指定一些测试用例，然后自主排查原因，自主修复，确保报表mcp的数据都是可以查的，可以基于tool描述来制定测试用例提问，看看是chat本身还是agent 还是skill或者是workflow，或者是前端动态加载ui哪方面还有缺陷和不足，阻塞了问数服务，然后做完资深产品专家，你指定一个迭代版本v0.6解决这个问题做为核心目标。
3.同时每次查出报表后，要补一个查异常的workflow，确保没有数据问题，或者是有数据问题会第一时间告知用户。
4.提需求，如果用户问的指标不在tool里覆盖，就需要告知用户当前不支持这个支持，已记录你的需求或者让用户进一步明确指标开发需求。如果是对接需求则是让用户提供对接文档，我们校验文档里的内容和用户提的对接需求是否匹配，采集对接则应该有数据，监测回传对接则应该有监测回传信息。如果监测回传文档做一轮解析，如果没有特殊操作，可调用配置mcp直接完成功能配置，用户可以立即创建监测链接。

