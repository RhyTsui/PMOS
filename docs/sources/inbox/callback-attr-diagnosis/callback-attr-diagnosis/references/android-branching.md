# Android Branching

Android 常见主链：

- `callbackMode=SDK`
  - 主证据：`SDK income + 804`
- `callbackMode=API`
  - 主证据：`feedback-res`

Android 常见默认经验：

- `PAY` 常看 `d_c_order_i`
- `804` 经常可以作为主闭环证据之一
- 关键归因身份更偏 `oaid`

不建议把这套经验直接迁移到 iOS。
