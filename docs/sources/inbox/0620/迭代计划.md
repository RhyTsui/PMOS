

1. 定义 Planner Shadow 完成标准：合法 PlannerPlanContract、状态可统计、无敏感泄露、不影响 route/tool/answer、默认 shadow。
2. Phase 3D-1：排查真实 API 下 Planner json\_parse\_failed 的真实原因。
3. 确认 planner\_shadow 是否真实调用模型，并记录 llm\_unavailable / timeout / empty\_response / short\_response / fallback\_response / non\_json\_response。
4. 修复 planner\_shadow debugSummary 不生成的问题。
5. 增加 parse\_error\_code 细分，但禁止记录 raw output / raw prompt / raw user message。
6. 重新跑真实 /api/chat 验证，确认主链不受 Planner 失败影响。
7. 修正 Planner Prompt，只允许输出符合 PlannerPlanContract 的 JSON object。
8. 清理 Planner Prompt 示例，移除 tool\_name / input\_mapping / output\_mapping 等未定义字段。
9. 对齐 plan\_steps、evidence\_mode、evidence\_requirements 与当前契约。
10. 用“你好 / 查日报 / 昨天巨量消耗多少”验证 Planner 输出稳定为 succeeded 或 contract\_validation\_failed。
11. 清理工作区，分类 modified / untracked 文件。
12. 将 P0 治理字段 canonicalId / aliasIds / deprecatedBy 等单独提交或暂存。
13. 将历史任务改动与 Planner 后续改动隔离，确保后续在干净工作区实施。
14. 单独修复 TypeScript baseline：ChatContainer.tsx、search-provider-config.ts、report-execution-integration.test.ts。
15. 确保 npx tsc --noEmit 全量通过。
16. 基于当前成果提交 Phase 3D，提交前明确 Planner 仍为 shadow，不接管主链。
17. Phase 3E：实现 Planner Shadow Metrics，统计 disabled / llm\_unavailable / timeout / json\_parse\_failed / contract\_validation\_failed / succeeded。
18. Phase 3E：补充平均耗时、succeeded rate、json\_parse\_failed rate、contract\_validation\_failed rate、task\_type 分布统计。
19. Phase 3F：实现 Planner 与现有 route 的 offline shadow comparison。
20. Phase 3F：对比 route.intent\_type、planner.task\_type、planner.service\_intent、planner.evidence\_mode、planner.confidence。
21. Phase 3F：输出 planner\_route\_alignment：matched / diverged / planner\_uncertain / existing\_uncertain。
22. 确认 Planner 差异只进 trace，不改变 route、tool、answer。
23. 完善 Prompt 后台治理，确认 planner\_shadow use case、active prompt、prompt version、required\_variables、forbidden\_variables 可见。
24. 补 Admin effective config view，展示 planner\_shadow 的模型配置、Prompt 配置、变量契约与生效来源。
25. P1-2：落地 Domain Ontology / Object Resolver，替换 semantic-frame-resolver.ts 中的业务对象硬编码识别。
26. 建立 Report Catalog / Domain Ontology / Capability Manifest 作为业务对象识别真源。
27. 验证 ROI报表 → report.roi。
28. 验证 查日报 → report.daily。
29. 验证 查看报表 不误判为 report.daily。
30. 验证 数据口径是什么 不生成 report object。
31. 验证 日报怎么配置 可识别 report.daily，但不进入 report execution。
32. 验证 联调 → workflow。
33. 验证 投放包 → package。
34. P1-3：迁移 request-understanding.ts 遗留关键词 fallback。
35. 改造 hasStrongReportIntent，优先消费 semanticFrame 和 domain object resolver。
36. 改造 inferServiceIntentFromRequirement，禁止直接用“日报/报表/数据/查数/取数/明细”决定 serviceIntent。
37. 改造 deriveUserRequirement 回填逻辑，避免关键词反向制造报表执行意图。
38. 改造 inferRequestedView，优先基于 semanticFrame / object / capability affordance 判断视图。
39. 每迁移一类 fallback，就补一组回归测试。
40. P1-6：建设 Field Dictionary，提供字段 key、displayName、枚举、类型、含义。
41. P1-6：建设 Metric Dictionary，提供 ROI、消耗、激活、留存等指标口径。
42. P1-6：建设 Schema Registry，提供报表字段、维度、类型、可视化结构。
43. 将 field\_definition 问题接入 Field Dictionary / Metric Dictionary / Schema Registry，而不是让 LLM 凭经验回答。
44. P1-7：补强 Capability Manifest 的 executionClass、supportedSemanticTasks、supportedObjectTypes。
45. P1-7：补强 Capability Manifest 的 requiredSlots、outputContract、riskLevel、capabilityPurpose。
46. 建立 report.daily + retrieve\_report\_data → get\_zt\_ad\_day\_report 的能力映射。
47. 建立 field\_definition + report.daily → schema\_lookup / field\_dictionary 的能力映射。
48. 建立 package.fetch + request\_operation → package\_fetch capability 的能力映射。
49. 建立 workflow.integration → integration\_workflow 的能力映射。
50. P1-3 后续：实现 capabilityRefs resolver 层，将 tool\_name 映射为真实 capabilityRefs。
51. 禁止直接用 tool\_name 做最终能力选择。
52. 新增 planner-capability-grounding.ts，只生成 Capability Candidate，不执行 MCP。
53. 新增 planner-capability-grounding.test.ts。
54. 将 PlannerPlanContract 映射为 Capability Grounding 输入。
55. 校验 Planner task\_type / service\_intent / evidence\_mode / candidate\_capabilities 是否能落到真实能力清单。
56. 标记 capability candidate 状态：executable / missing\_input / permission\_blocked / unsupported。
57. 确认 Planner candidate\_capabilities 只能作为 hint，不能决定最终能力。
58. 新增 planner-tool-contract-matching.ts。
59. 新增 planner-tool-contract-matching.test.ts。
60. Tool Contract Matching 校验 toolPurpose。
61. Tool Contract Matching 校验 supportedServiceIntents。
62. Tool Contract Matching 校验 requiredInputs / optionalInputs / defaultInputs。
63. Tool Contract Matching 校验 resolverDependencies。
64. Tool Contract Matching 校验 permission requirements。
65. Tool Contract Matching 校验 project / app / media support。
66. Tool Contract Matching 校验 outputContract。
67. Tool Contract Matching 校验 errorTaxonomy。
68. 确认 Planner tool\_selection\_priors 只能加权候选，不能成为 selectedTool。
69. 建立 Capability Candidate → Executable Candidate → Tool Contract Matching → Resolver / Preflight 的链路。
70. 接入 Resolver Chain：Context Resolver → Entity Resolver → Dictionary Resolver → Default Policy → Required Input Assist。
71. 确认 Tool 默认策略以 Tool Contract 为准，不在 Chat Core 写业务默认值。
72. 确认 Prompt 不写死媒体、指标、项目。
73. 确认 LLM 不直接生成 appId / media\_id / tool args。
74. Required Input Assist 只负责解释缺什么、为什么缺、如何追问。
75. P1-8：实现 Tool Fallback Policy。
76. 建立 Candidate → Executable Candidate → Selected Capability → Preflight → Execute → Normalize Error → Fallback Policy。
77. permission\_blocked 不 fallback，直接提示权限问题。
78. unsupported\_project 允许 fallback 到其它能力或工具。
79. empty\_data 支持 fallback 或返回空数据说明。
80. business\_failed 按 inner code 分类归一化。
81. schema\_mismatch 不执行工具，进入 contract error。
82. 接入 Tool Execution + Evidence Ledger。
83. 工具执行后的事实全部写入 Evidence Ledger。
84. Planner inference 只能作为 planner\_inference evidence，不能伪装成 tool\_result。
85. Answer Composer 只基于 Evidence Ledger 生成用户可读答案。
86. Answer Composer 区分已确认事实、高概率推断、待验证问题。
87. 接入 ContractSafety 后置硬闸。
88. ContractSafety 检查无证据业务断言。
89. ContractSafety 检查工具结果是否被模型改写反。
90. ContractSafety 检查 sourceRefs / evidenceRefs 缺失。
91. ContractSafety 检查 raw params、appId、media\_id、project\_id 是否泄露到主消息。
92. ContractSafety 检查乱码、敏感信息、失败说成成功、shadow plan 当成真实执行。
93. 做 Result / Runtime / Disclosure 分离，主消息只展示结果和必要阻断。
94. 右侧展示过程、参数、工具、Prompt、Trace、来源、原始信息。
95. P1-10：实现 DataVizRenderer columnSchema。
96. 保持 columns: string\[] 兼容。
97. 新增 columnSchema: \[{ key, label, type, visible }]。
98. Renderer 优先展示 label，缺失 label 时 fallback 到 key。
99. 禁止使用 as any 解决 columnSchema 类型问题。
100. 增加 Browser E2E，覆盖真实 UI、右侧过程、刷新回放、Network/Console、乱码扫描。
101. 增加真实 /api/chat regression，覆盖 Planner Shadow、Capability Grounding、Tool Matching、Fallback、Evidence Ledger。
102. 增加 Admin Control Plane 可观测能力。
103. Admin 展示 ModelUseCase：planner\_shadow / planner\_route\_review / planner\_answer\_compose。
104. Admin 展示 Prompt version、Prompt variables contract、Effective model config。
105. Admin 展示 Capability Manifest、Tool Contract、Resolver Policy、Evidence Policy、Safety Policy。
106. Admin 展示 Feature Flags、Trace Sampling、Shadow Success Rate。
107. Planner Assisted Routing 第一阶段只做 route review，不覆盖 route。
108. Planner Assisted Routing 第二阶段只在 Planner 与规则一致时提高 confidence。
109. Planner Assisted Routing 第二阶段只在 Planner 与 capability grounding 一致时补充候选。
110. 禁止 Planner 直接覆盖 route.intent\_type。
111. 禁止 Planner 直接选择 MCP tool。
112. 禁止 Planner 直接生成 tool args。
113. 禁止 Planner 直接宣告执行成功。
114. P2-2：建设 Automation Task Runtime。
115. 新增 AutomationTask / TaskRun / TaskArtifact / TaskNotification。
116. 新增 Scheduler / Worker / Run History。
117. 将定时报表、ROI 异常检查、联调监控、投放包检查从同步 chat 中拆出。
118. P2-3：在 Semantic Frame、Domain Ontology、Capability Affordance、Evidence Ledger、Execution Gate、Risk Approval 稳定后，再做数字优化师 Agent 化。
119. 规划数据归因 Agent。
120. 规划创意策略 Agent。
121. 规划诊断 Agent。
122. 规划投放策略 Agent。
123. 多 Agent 只能基于受控能力、证据账本、执行门禁和风险审批运行。
124. 最后评估 Planner-first 主链接管策略，但必须先完成 Shadow、Grounding、Tool Matching、Resolver、Evidence、Safety、E2E 和 Admin 治理闭环。

