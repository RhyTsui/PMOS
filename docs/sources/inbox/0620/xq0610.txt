 E:\AI\ai-os\docs\sources\inbox\小乔智投测试集v1.1.xlsx 一条条测 一条条改 走通等我确认后进行下一条。


天气
新闻解读
行业文档解读
行业知识问答
系统知识问答
系统操作帮助
指标解释
查询实时数据.大盘、媒体、应用类型、团队、自然量、包、活动、广告
查询昨日数据 终端
查询近7日数据
查询近30日趋势、分日
上传图片 上传视频 上传excle模板 上传有数据的excle
粘贴数据 粘贴图片 
拖拽图片 拖拽视频 拖拽excle模板 拖拽有数据的excle
解读数据
提取数据 生成excle
定时任务
取包
分包
查看包状态
查看包联调记录
发起联调
查看联调进度
检查2个排查skill是否可调用
智投数据和媒体数据不一致
智投数据和魔方不一致
智投各报表数据不一致
归因诊断
回推诊断
查看top素材、素材、设计师
粘贴素材ID 分析素材数据
用户需求多轮沟通确认转需求流程
对接新媒体

前端Skill触发测试  
MCP能力验证
联网搜索MCP  完整链路验证
IntentOrch增强层  完整链路验证





















一、自建联网搜索api然后完成联网搜索mcp配置 和调用链路测试
二、在当前项目中集成 IntentOrch 作为增强层，不改现有链路，支持多 MCP，复用系统模型配置。
接入位置：第 2 步「意图理解」（requirement drafting）完成后，第 3 步「能力发现」之前。
具体位置：route.ts 中 ~L1324 附近的 requirement drafting 函数调用之后。
实施步骤：
1. npm install @mcpilotx/intentorch
2. 新建 intent-orch-enhancer.ts，包含：
   - 复用系统模型配置
   - 批量连接所有 MCP Server
   - 异步增强执行函数
3. 在 route.ts 的意图理解代码后面，添加异步增强调用
4. 传入：原始用户问法 + 意图对象 + 对话历史
5. 通过环境变量 INTENT_ORCH_ENABLED 控制开关
6. 增强失败静默忽略，不影响主链路
请直接分析代码并实施。

MCP检查和说明
媒体配置mcp 没有skill绑定吧， 依赖key调用，后续迭代，已停用。
自动联调MCP 没有skill绑定吧，依赖key调用，后续迭代，已停用。
统一采集网关MCP 没有skill绑定吧，依赖新增tool，后续迭代，已停用。
报表编排MCP 没有skill绑定吧，生成的假服务，服务弃用，已停用。

联网搜索MCP 没配置所以未连接，需自建MCP 服务来调搜索 API，再配置。
排查MCP 正常连接 
智投配置MCP 正常连接
报表MCP 正常连接
监测链接MCP 正常链接
运维调度MCP 正常连接
归因MCP 正常连接

SKILL检查和说明
后台只看到2个skill一个要弃用的，一个是今天增加的用于排查的。
而审计出来的23个skill，中风险、高风险的是什么风险，要处理掉。
低风险15个只读，是干什么，会不会错误调用，是否有业务价值，没有也要处理。当前第一轮集成测试，通过率为0，是否和这些skill有关。都需要验证。我准备集成IntentOrch 作为增强层，是不是有些skill就不需要。
report-orchestration这个skill是用于自动任务的嘛？如果一定要有，就需要完善。

左侧侧边栏没有吃到 会话区背景，这个之前修复后被回退了。

PC模式下在一定宽度时：
1.logo欢迎语模块会被藏在输入框下层遮挡住，
2.底部大模型状态和文案也会错位，需要自适应大模型状态单独一行展示。
实现描述：欢迎区外层容器是垂直 Flex 布局，居中对齐，最小高度 184px；Logo 容器两层嵌套；图片组件是图片绝对定位居中，父容器相对定位，固定尺寸 72x72px。

描述一下skill的能力和触发场景触发词，有没有被能力发现，以及怎么生成的：
package_delivery_execution_skill
report_template_builder_skil
scheduled_report_skill
report-orchestration
tracking_link_delivery_skill
integration_requirement_work 
metric_requirement_intake_skill

禁用auto_debug_skill

低风险这15个skill的确都锁定了一些需求场景，但是很多mcp或能力都没支持，本身就只是一个提示词嘛？还是一个空文件？有实际运行嘛？这些skill有没有必要存在，要治理，尤其是提示词，请分析。

页面打开报错：Module not found: Can't resolve '@/contracts/skills/android-attribution-callback-diagnosis'

问题6：排查数据MCP        │ ⏸️ 需配置endpoint排查数据MCP
解决思路：之前安装了skill，但是mcp里的tool名称都改了。现在基于最新的排查mcp的tool，我更新了提示词，在打包文件中E:\AI\ai-os\docs\sources\inbox\callback-attr-diagnosis.zip  APP归因回推问题排查skill
请分析这个skill，评估可用性，和我确认是否需要微调，再完成能力发现注册，编排，正确调用mcp，验证链路走通。（暂时管理中心导入乱码编排功能异常等问题） 充分评估后最好和我确认实施步骤

为什么欢迎语展示“
我是小乔，你的 AI 投放服务台。可以帮你看数据、查异常、验流程、拼报表、提需求、问逻辑、追情报。‘，是哪里写死的，不是读取后台配置的提示语列表吗，刷新之后也没更新。

先问天气，回复“百度搜索近30天的激活数”。再问“百度搜索近30天的激活数 ”，回复没有公开数据。并且多出了一个新会话，历史会话空白。请审查会话列表和会话加载的BUG

移动端新会话空态时页面出现了右侧滑动条，高度应该是一屏，其次底部不应该存在白底占位区域，再就是选择项目时页面会被放大选中后不能还原。

1.清除用户的历史会话消息
2.因为部分历史消息无法渲染展示跳转到了新会话空态页，应该拒绝这种方式，改为给用户提示真实原因，并且思考如何避免和后续迭代中历史会话不可读、如何立即改善提示、如何避免跳转空态页、以及后续迭代如何让历史会话更健壮
1、立即改善：拒绝跳转空态页，改为明确提示，停留在当前历史会话页面，在聊天区域展示一条系统提示：
{部分历史消息无法正常渲染
原因：某条消息格式不兼容（如旧版本插件、自定义卡片类型已下线）。
你可以：
继续查看其他能正常展示的消息
尝试「复制原始内容」查看完整对话
如反复出现，请联系支持并告知会话 ID}
2、避免跳转空态页：在渲染层做容错与降级，逐条消息渲染 + 容错边界
不要整段 JSON / 整个会话一次渲染
每条消息用错误边界包裹
当某一条消息渲染失败时：
该消息位置显示一个“该消息内容暂无法展示”的占位块
提供「查看原始数据」或「复制 JSON」
其他历史消息正常渲染

 对已知不可渲染的类型做降级
比如：
已下架的卡片类型 → 降级为纯文本摘要
缺失的自定义组件 → 降级为 JSON 展示
未知的 role / 结构 → 降级为“不支持的消息类型”

1.刚才把分享添加的按钮颜色变灰了一些，改到左侧边栏折叠态的添加图标了，不过没问题，把左侧边栏折叠态的搜索和会话历史图标也一并改同一种灰色吧。
2.刚才让左侧边栏吃到了会话区背景，现在让会话区顶部栏也吃到会话区背景。
3.欢迎语后台支持后台配置1-50个，每次用户加载随机展示不同的欢迎语。

新会话空态光晕散开入场动效

1.推荐提问：经过意图路由和业务流引导
2.标题生成：合并prompt构建逻辑，减少冗余调用，确保功能有效
3.联网搜索：添加 web search MCP 服务，走通能力发现全部链路
4.知识库-受控词典：用于意图理解、实体解析等环节。
5.数据分析：上传或粘贴或输入数据且意图是分析数据、调用工具返回数据等有数据输入的场景，调用数据分析的提示词yongLLM解读数据，推荐动作基于上下文提供推荐动作引导。先写一个通用的简单的数据分析提示词放在管理中心提示词管理处。
6.排查数据：MCP服务未连接，
7.视频封面：采用纯前端的快速方案
8.意图路由：昨天不是优化了LLM调用吗请审查；把“基于规则的硬编码路由”改为“LLM主导决策，规则提供辅助信息”的
9.文件理解：当前大模型支持多模态解析，使用llm理解图片、视频、文档，确保各文件内容能解析并转化为输入。
10.报表编排：去掉报表编排 MCP：承载自动报表模板解析、指标校验、数据预览和定时任务创建能力。改为Skill，当要用模板取数，可以走这个skill，skill可可调用各个报表tool。做一个skill并集成到后台。
步骤：
report.parse_template
从文本模板或标准二维 Excel 模板中提炼报表结构、维度、指标、频率和接收对象。
report.validate_metrics
校验报表模板中的指标是否存在、口径是否明确，并返回问题指标列表。
report.preview
基于已确认模板调用多个报表/数仓工具拉取数据并返回类 Excel 数据预览。
scheduled_report.create
基于已确认模板创建定时报表任务。
11.管理中心-能力中心-接入管理：删除统一采集网关 MCP
