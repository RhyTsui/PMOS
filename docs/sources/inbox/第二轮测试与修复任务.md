任务目标：第二轮测试与修复，从测试集第一条用例开始，按顺序逐条推进，完成端到端验证、问题定位、设计方案、实施修复、复测记录与提交。

本轮不是静态审查，也不是批量跳测。必须使用 Playwright 吊起真实浏览器访问真实系统，从第一条用例开始执行。第一条用例确认完整通过后，才能进入下一条。禁止跳测、批量跳过、只做接口推断、只看表面答案通过。

一、基础信息

测试集：

E:\AI\ai-os\docs\sources\inbox\小乔智投测试集v1.1.xlsx

测试系统地址：

http://10.236.14.27:8002/

Playwright 依赖路径：

E:\AI\ai-os\node_modules

启动方式：

在项目对应目录执行：

npm run dev

实际启动 tsx src/server.ts。

前端端口：8002。

Next.js dev server 需要跑在 0.0.0.0:8002，监听所有网络接口。

登录方式：

使用 Playwright 打开浏览器页面。首次需要人工扫码登录一次，登录成功后保存并复用登录态。后续测试不得重复要求扫码，除非登录态失效。

二、执行原则

1. 当前只从第一条用例开始。
2. 每条用例必须完成：

验证 → 定位 → 方案设计 → 修复 → 复测 → 记录 → 提交

3. 第一条未通过前，禁止进入第二条。
4. 禁止为了通过用例写死文本、if/else 特判、关键字硬编码、单样例兜底。
5. 禁止绕过总纲、绕过通用 Chat 架构、绕过契约、绕过 renderer、绕过 right panel disclosure。
6. 禁止用 UI 层兜底修后端业务逻辑。
7. 禁止破坏通用 Chat 定位。小乔智投是广告业务域能力接入，不是广告报表专用脚本系统。
8. 禁止引入乱码、错码、问号替代字符、mojibake、HTML 实体误解码、中文编码错误。
9. 所有修复必须符合总纲规范、语义契约、能力编排、Evidence/Source、Trace、Renderer、History Replay 的一致性要求。
10. 每条用例通过后，必须围绕该用例的设计目的自行补测，确认不是写死或硬编码，而是真正通用实现。

三、测试前准备

1. 读取并理解测试集第一条用例，明确：

   * 用户输入
   * 预期结果
   * 涉及能力域
   * 需要触发的 runtime 链路
   * 需要展示的主消息内容
   * 右侧过程与依据应该展示什么
   * 需要校验的接口字段
   * 刷新回放预期
   * 标题生成预期
   * 是否涉及工具调用、source/evidence、semantic_result、business_summary、answer_markdown

2. 启动或确认测试系统可访问：

   * http://10.236.14.27:8002/ 能打开
   * 页面加载的是当前代码
   * 无旧构建、旧缓存、旧服务残留
   * 如服务未启动，按项目脚本启动
   * 如端口被占用，定位占用进程后处理

3. 使用 Playwright 打开真实浏览器页面。

4. 人工扫码登录一次，保存登录态。

5. 登录后进入测试页面，确认用户可以输入消息并发送。

四、每条用例验收标准

每条用例必须同时通过以下 10 类验收：

1. 页面链路通过

用户输入、发送、等待、结果展示、AI 占位、loading、右侧执行过程、日志、提示词、操作状态均正常。

特别检查：

* 用户消息是否点击发送后即时落位
* 不等待后端返回才展示用户消息
* AI 占位是否及时出现
* loading 是否正常
* 主消息是否只展示用户需要的结果、结论或必要阻断
* 主消息不得泄露内部协议、内部字段、调试结构

2. 接口读回通过

检查真实接口 response 结构完整，字段以最新链路契约为准，至少包括并核对：

* message_id
* thread_id
* trace_id
* routeDecision
* semanticFrame
* userRequirement
* executionGate
* toolCalls
* semantic_result
* business_summary
* answer_markdown
* source/evidence
* tool 信息
* renderer consumption 信息
* right panel disclosure 信息

重点检查：

* 主消息是否泄露内部字段
* 右侧过程是否正确展示
* sourceRefs/evidenceRefs 是否可读
* tool calling 状态是否一致
* semantic_result 与展示结果是否一致

3. 刷新回放通过

刷新页面或重新进入会话后，必须正确回放：

* 历史用户消息
* AI 主消息
* 结果卡片
* 右侧过程
* 工具调用状态
* 日志与提示词
* 标题
* 来源与证据
* trace 状态
* renderer 消费状态

不得出现刷新后丢卡片、丢右侧过程、丢来源、标题变空、状态错乱。

4. 标题 LLM 正常

会话标题必须由标题生成链路产生或更新。

不得出现：

* 空标题
* 乱码标题
* 内部协议标题
* trace_id / message_id / routeDecision 等内部字段标题
* 固定写死标题

5. Network / Console 健康

浏览器 Console 不得出现非预期异常错误。

Network 不得出现非预期 4xx / 5xx。

SSE 流必须稳定：

* 不异常中断
* 不丢包
* 不输出内部调试协议
* 不输出乱码
* 不输出未消费的内部 contract

6. DOM / SSE / API / 日志乱码扫描通过

必须检查以下位置：

* 页面 DOM
* SSE chunk
* API response
* 浏览器 Console
* 服务端日志
* 右侧过程日志
* 标题
* 历史回放内容

不得出现：

* 乱码
* 错码
* 问号替代字符
*  
* mojibake
* HTML 实体误解码
* 中文被错误编码
* UTF-8 / GBK 混乱
* 中文路径或中文字段显示异常

7. 静态门禁通过

对本次修改文件执行编码与静态检查：

* UTF-8 无 BOM
* 无乱码风险
* 无问号替代字符
* 无硬编码
* 无单样例 if/else
* 不破坏通用 Chat 架构
* 不破坏 ResponseContract / SemanticResultContract 兼容字段
* 不绕过契约兜底
* TypeScript / lint / test 按项目现有命令通过

8. 链路健康通过

必须确认以下链路状态一致：

* trace
* tool calling
* renderer consumption
* message projection
* right panel disclosure
* history replay
* title update
* source/evidence ledger
* semantic_result
* business_summary
* answer_markdown

不得只看表面答案通过。

9. 展示体验通过

返回消息必须经过排版优化，展示体验良好：

* 主消息清晰、简洁、可读
* 不堆内部过程
* 不展示 raw params
* 不展示 query_contract / slot_validation / summary 等内部枚举
* 右侧过程承载执行链路、工具、Prompt、日志、来源、原始信息
* 卡片与正文分工明确

10. 反硬编码补测通过

每条用例通过后，围绕该用例设计目的自行补测至少 1-2 条相邻表达，确认不是写死。

例如：

* 同义表达
* 时间表达变化
* 媒体名称变化
* 指标别名变化
* 缺少部分上下文时的追问
* 数据为空或工具失败时的兜底

五、失败处理要求

如果第一条用例失败，必须先定位，再修复。

定位必须具体到：

* 具体文件
* 具体函数
* 具体链路阶段
* 具体契约字段
* 具体 renderer 或 projection
* 具体 API response / SSE chunk / DOM 节点
* 具体 Network / Console 错误
* 具体编码风险文件

禁止只写“可能是前端问题”或“疑似后端问题”。

定位后必须输出问题分析：

1. 问题现象
2. 复现步骤
3. 失败链路
4. 根因判断
5. 影响范围
6. 是否属于通用 Chat Core 问题
7. 是否属于广告业务域能力问题
8. 是否涉及契约缺失
9. 是否涉及 renderer 未消费
10. 是否涉及历史回放或标题链路
11. 是否涉及编码风险

六、方案设计要求

修复前必须先给出设计方案。

方案必须符合：

1. 总纲规范
2. 通用 Chat 服务定位
3. Fat MCP / Fat Skill + Thin Chat Runtime
4. Tool Contract / Capability Manifest / SemanticResultContract 边界
5. 主消息与右侧过程的展示分工
6. Evidence / Source / Trace 可观测要求
7. History Replay 一致性
8. Prompt 不能代替工具、权限、事实和计算
9. UI 不兜底后端业务逻辑
10. 不为单条测试用例写死

方案必须说明：

* 修改哪些文件
* 为什么改这些文件
* 是否改 runtime
* 是否改 prompt
* 是否改 renderer
* 是否改 contract
* 是否改 tests
* 是否影响历史会话
* 是否影响其他业务域
* 如何验证不是硬编码

七、实施修改要求

实施时必须：

1. 小步修改，避免大范围无关重构。
2. 保持现有契约兼容。
3. 不修改无关文件。
4. 不格式化全库。
5. 不提交 .bak、临时文件、登录态文件、截图缓存。
6. 不破坏现有测试。
7. 修改后立即执行必要静态检查。
8. 修改后重新跑第一条完整链路。

八、复测要求

修复后必须完整重跑第一条用例，不允许只测失败点。

复测必须覆盖：

1. 新会话发送
2. 用户消息即时落位
3. AI 占位和 loading
4. 主消息结果
5. 右侧执行过程
6. 右侧日志和提示词
7. 接口 response
8. SSE chunk
9. Network
10. Console
11. DOM 乱码扫描
12. API 乱码扫描
13. 服务端日志乱码扫描
14. 刷新回放
15. 标题生成
16. 相邻表达补测
17. 静态门禁
18. 测试记录

九、测试记录要求

每条用例完成后，必须记录：

* 用例编号
* 用户输入
* 预期结果
* 实际结果
* 涉及能力域
* 页面链路结论
* 接口读回结论
* 刷新回放结论
* 标题 LLM 结论
* Network / Console 结论
* DOM / SSE / API / 日志乱码扫描结论
* trace / tool / renderer / message projection / right panel disclosure 结论
* 是否修复
* 修改文件
* 根因
* 方案摘要
* 复测结果
* 相邻表达补测结果
* 是否通过
* commit hash

记录应写入项目既有测试记录位置；如项目没有明确位置，先创建或更新本轮测试记录文档，并说明路径。

十、提交要求

每条用例最终通过后，单独提交。

提交前检查：

* git diff 只包含本用例相关修改
* 无临时文件
* 无 .bak
* 无登录态文件
* 无截图缓存
* 无乱码
* 静态检查通过
* 复测记录已更新

commit message 建议格式：

fix(e2e): pass round2 case 001 chat runtime validation

或根据实际问题选择：

fix(runtime): ...
fix(presentation): ...
fix(history): ...
fix(trace): ...
fix(encoding): ...
fix(renderer): ...
test(e2e): ...

十一、当前执行顺序

请严格按以下顺序执行：

1. 读取测试集第一条用例。
2. 输出第一条用例的输入、预期、涉及能力和验收点。
3. 启动或确认系统可访问。
4. 使用 Playwright 打开浏览器。
5. 人工扫码登录一次并复用登录态。
6. 执行第一条端到端测试。
7. 同步抓取并检查 Console、Network、SSE、DOM、API response、右侧过程、标题、刷新回放。
8. 如失败，定位具体链路与字段。
9. 输出问题分析和符合总纲的设计方案。
10. 实施修改。
11. 执行静态检查和编码检查。
12. 重跑第一条完整链路。
13. 做相邻表达补测，确认非硬编码。
14. 更新测试记录。
15. 提交。
16. 等待确认或继续第二条。

十二、最终输出格式

完成第一条后，输出：

1. 第一条用例信息
2. 执行结果
3. 发现的问题
4. 根因分析
5. 设计方案
6. 修改文件
7. 验证结果
8. 乱码扫描结果
9. 刷新回放结果
10. 标题生成结果
11. 相邻表达补测结果
12. 是否通过
13. commit hash
14. 下一条是否可以开始

注意：本任务从第一条开始逐条推进。第一条未完整通过前，不得进入第二条。
