# Version Planning

此目录存放版本分析与候选规划，不代表当前最终决策。

适合放入：

- `v0.3` / `v0.4` / `v0.6` 的版本推演
- 多个候选路线图
- 愿景扩展稿
- 尚未采纳的升级分析

规则：

- 默认视为输入源
- 新文档先进入这里，再进入决策收敛
- 若某个版本方案被采纳，应新增或更新 `docs/decisions/`，而不是仅靠这里的文件名表示“生效”
