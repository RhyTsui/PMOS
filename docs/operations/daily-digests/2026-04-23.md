# 2026-04-23 对话交接记录

更新时间：2026-04-23 15:28:40 +08:00

## 本轮用户要求

- 系统定位明确为“一个项目的整体介绍 wiki 文档演示系统”。
- 不要切到别的项目，继续使用 `E:\AI\ai-os`。
- 不要新起端口，统一使用 `http://localhost:4312/`。
- 所有人看的文档放在一起。
- 本地保留两份共享文档：
  - 原文文档
  - 中文文档
- 需要更炫一点的前端效果，方向是“交互式全景图 / 知识图谱”，不是静态海报。
- 不要只出清单，要直接产出工程并可查看结果。

## 已落地改动

### 1. Wiki 统一文档区

已在 `src/backend/server.ts` 中接入共享文档能力：

- 页面：`/pmaios/wiki`
- 接口：
  - `GET /api/wiki/shared-documents`
  - `PUT /api/wiki/shared-documents`
- 本地存储文件：
  - `generated/wiki/shared-documents.json`

当前语义：

- 所有人访问 `/pmaios/wiki` 看到的是同一份共享内容。
- 页面中保留两份文档：
  - 原文文档
  - 中文文档

### 2. 交互式全景图 / 知识图谱页

已在 `src/backend/server.ts` 中新增：

- 页面：`/pmaios/graph`
- 数据接口：`/api/graph/overview`

当前图谱页能力：

- 全屏式画布布局
- 节点卡片
- 连线
- 点击节点后右侧详情面板
- 节点内容覆盖：
  - PMAIOS / Workspace / Wiki / Graph
  - Kernel / Requirements / Progress Protocol
  - Workflow / Modes / Codex Migration
  - Version Plan / Execution Checklist / Transition Snapshot
  - Shared Source Doc / Shared Chinese Doc
  - Boards
  - 前 10 条 prompt 条目

### 3. 统一入口已接入图谱

首页 `/` 已增加：

- `PMAIOS 全景图谱`

Wiki `/pmaios/wiki` 已增加：

- 图谱地址展示：`http://localhost:4312/pmaios/graph`
- Knowledge Graph 区块中增加跳转说明

## 已核对过的状态

- `npm.cmd run build` 已通过。
- `http://localhost:4312/` 在恢复后可返回 `200`。
- `http://localhost:4312/pmaios/graph` 在恢复后可返回 `200`。

## 当前运行状态

为了让服务恢复可访问，后端已改为通过独立 PowerShell 宿主启动，而不是依赖当前短生命周期会话。

最近一次确认：

- 端口 `4312` 已监听
- 根路径和图谱页都能返回成功

但要注意：

- 之前出现过“进程启动后未稳定常驻”的情况。
- 如果重启机器后再次打不开，先优先检查 `4312` 是否在监听，而不是先怀疑页面代码。

## 重启后建议先做的动作

1. 打开：
   - `http://localhost:4312/`
   - `http://localhost:4312/pmaios/wiki`
   - `http://localhost:4312/pmaios/graph`
2. 如果打不开，先检查 `4312` 端口监听状态。
3. 如果端口没起来，优先从 `E:\AI\ai-os` 启动：
   - `node E:\AI\ai-os\dist\backend\src\backend\server.js`

## 下一步待做

- review 你之前定下的“提炼策略 / 记忆 / 上下文 / 多版本需求”，确认重装系统后缺失了哪些能力。
- 基于现状补一个真正的 `v0.5 fix`：
  - 对话提炼是否自动入库
  - 长期记忆是否自动同步
  - 文档、图板、wiki、graph 是否形成统一入口
  - 哪些旧能力仍是 partial / missing

## 关键文件

- `E:\AI\ai-os\src\backend\server.ts`
- `E:\AI\ai-os\generated\wiki\shared-documents.json`
- `E:\AI\ai-os\docs\operations\current-version-progress.md`
- `E:\AI\ai-os\docs\operations\v0.4-v0.5-execution-checklist.md`
- `E:\AI\ai-os\docs\operations\v0.5-implementation-index.md`
- `E:\AI\ai-os\docs\operations\codex-collaboration-migration-baseline.md`
- `E:\AI\ai-os\docs\operations\codex-collaboration-migration-manifest.json`
