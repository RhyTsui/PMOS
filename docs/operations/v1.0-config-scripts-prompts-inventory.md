# PMOS v1.0 config / scripts / prompts 细盘

- version: v0.1
- date: 2026-05-09
- status: active
- owner: product office

## 1. config

### 1.1 应保留

1. `config/product-management/agent-blueprints.json`
2. `config/product-management/product-method-registry.json`
3. `config/product-management/README.md`
4. `config/providers.md`
5. `config/env.example`

### 1.2 应 sample 化

1. `config/providers.json` -> 不建议直接作为公开产品仓默认样例
2. 新增 `config/providers.example.json` 作为公开样例

原因：

1. `providers.json` 里带有当前企业代理地址
2. 产品仓应暴露结构，不应默认绑定内部环境

## 2. scripts

### 2.1 建议保留

1. `audit-frontend-workbench-style.mjs`
2. `run-playwright-frontend-verification.mjs`
3. `dingtalk-export-flow.example.json`

### 2.2 条件保留

1. `capture-screen.ps1`
2. `dingtalk-nightly-job.ps1`
3. `dingtalk-ui-automation.ps1`
4. `dump-dingtalk-ui.ps1`
5. `register-dingtalk-nightly-task.ps1`

条件：

1. 若产品仓首版把 DingTalk 自动化视为可选集成，则保留在 `scripts/optional/`
2. 若首版不承诺该能力，则不进入产品仓

### 2.3 默认排除

1. `generate-weekly-report-0509.cjs`

原因：

1. 文件名带具体日期
2. 更像一次性业务脚本

## 3. prompts

### 3.1 建议整体保留

当前 `prompts/` 目录里的文件都属于平台工作流提示资产：

1. `architecture_prompt.md`
2. `industry_analysis_prompt.md`
3. `prd_prompt.md`
4. `review_prompt.md`
5. `task_prompt.md`
6. `prompts/product-management/*`

### 3.2 后续要求

1. 需要补一份 prompts 索引文档
2. 需要标明哪些是平台默认 prompt，哪些是可替换 prompt
3. 不应在 prompt 内引用只能从本机会话得知的隐式上下文

## 4. 当前判断

截至 `2026-05-09`：

1. `config` 发布规则：`partial`
2. `scripts` 保留范围：`partial`
3. `prompts` 产品仓适配度：`partial`
4. `runtime-status` 样例：`solved`
5. `cloud-mirror` 样例：`solved`

## 5. 下一步

1. 用 `providers.example.json` 替代公开产品仓默认 provider 样例
2. 后续在产品仓里将可选脚本与核心脚本分目录
3. 为 `prompts/` 增加平台级索引与分类说明
