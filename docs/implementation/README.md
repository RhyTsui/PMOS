# Implementation

此目录用于存放 **已确认的实现说明** 与 **运行期生成的实现产物**。

它不应该继续承担“输入源文档混放区”的职责。

建议只保留以下两类内容：

- 当前生效的实现基线说明
- `run-*.md` 这类与具体执行实例绑定的实现产物

当前生效版本基线：

- `docs/implementation/PMAIOS_v0.6_核心定义.md`

以下内容应逐步迁出到 `docs/sources/`：

- 人工版本分析
- 候选愿景稿
- 多轮未定稿的版本规划
- 仅用于输入而非定稿的 `.docx` / `.md`
