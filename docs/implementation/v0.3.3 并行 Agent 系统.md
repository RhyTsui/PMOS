# PMAIOS v0.3.3 并行 Agent 系统

## 系统定位

**Parallel Agent Pool + Redis Queue + LangGraph 编排系统**（接近生产级 Agent OS 架构）

目标：将 v1 的"单循环 Agent"升级成：
- 🚀 多 Worker 并行 + 可恢复 + 可编排 DAG 的 Agent 集群系统
- ⏱️ 可连续运行 8 小时以上

---

## 架构总览

```
                 ┌──────────────────────┐
                 │   Scheduler (Cron)   │
                 └─────────┬────────────┘
                           ↓
                ┌──────────────────────┐
                │   Redis Task Queue   │
                │  (Queue + Streams)   │
                └─────────┬────────────┘
                          ↓
     ┌────────────────────────────────────────┐
     │         Worker Agent Pool (10~50)      │
     │   Claude Workers (parallel execution)  │
     └───────────┬───────────────┬───────────┘
                 ↓               ↓
        ┌──────────────┐  ┌──────────────┐
        │ LangGraph DAG │  │ Memory Layer │
        │ Orchestration │  │ Chroma/SQL   │
        └──────┬───────┘  └──────┬───────┘
               ↓                 ↓
         ┌────────────────────────────┐
         │  Result Aggregation Agent  │
         └────────────────────────────┘
```

---

## 技术选型

| 模块 | 技术 |
|------|------|
| Queue | Redis Streams / RQ / Celery |
| Orchestration | LangGraph |
| LLM | Claude API |
| Memory | Chroma / SQLite |
| Parallelism | asyncio + worker pool |

---

## 项目结构

```
agent-os-v2/
 ├── scheduler.py
 ├── worker_pool.py
 ├── redis_queue.py
 ├── graph/
 │    ├── workflow.py
 │    ├── nodes.py
 ├── agents/
 │    ├── planner.py
 │    ├── worker.py
 │    ├── reviewer.py
 │    ├── synthesizer.py
 ├── memory/
 │    ├── vector.py
 │    ├── state.py
 ├── config.py
 └── main.py
```

---

## 核心模块

### 1. Redis 任务队列

```python
import redis
import json

r = redis.Redis(host="localhost", port=6379, decode_responses=True)

QUEUE_KEY = "agent_tasks"

def push_task(task: dict):
    r.lpush(QUEUE_KEY, json.dumps(task))

def pop_task():
    task = r.rpop(QUEUE_KEY)
    if task:
        return json.loads(task)
    return None

def queue_size():
    return r.llen(QUEUE_KEY)
```

### 2. Worker Pool（10~50 并行）

```python
import asyncio
from agents.worker import WorkerAgent
from redis_queue import pop_task

class WorkerPool:

    def __init__(self, size=20):
        self.size = size
        self.workers = [WorkerAgent(worker_id=i) for i in range(size)]

    async def run_worker(self, worker):
        while True:
            task = pop_task()
            if not task:
                await asyncio.sleep(2)
                continue
            print(f"[Worker {worker.worker_id}] executing {task['id']}")
            result = worker.run(task)
            worker.save_result(task, result)

    async def start(self):
        await asyncio.gather(
            *[self.run_worker(w) for w in self.workers]
        )
```

### 3. Worker Agent（Claude 执行单元）

```python
import anthropic

client = anthropic.Anthropic()

class WorkerAgent:

    def __init__(self, worker_id):
        self.worker_id = worker_id

    def run(self, task):
        prompt = f"""
You are Worker Agent #{self.worker_id}

Task:
{task["content"]}

Return structured output.
"""
        res = client.messages.create(
            model="claude-3-5-sonnet-latest",
            max_tokens=2000,
            messages=[{"role": "user", "content": prompt}]
        )
        return res.content[0].text

    def save_result(self, task, result):
        print(f"[DONE] {task['id']}")
        # TODO: write to memory / DB
```

### 4. LangGraph 编排

```python
from langgraph.graph import StateGraph
from agents.planner import PlannerAgent
from agents.reviewer import ReviewerAgent
from agents.synthesizer import SynthesizerAgent

def build_graph():
    graph = StateGraph(dict)

    planner = PlannerAgent()
    reviewer = ReviewerAgent()
    synthesizer = SynthesizerAgent()

    def plan_node(state):
        return planner.create_tasks(state)

    def review_node(state):
        return reviewer.review(state)

    def synth_node(state):
        return synthesizer.merge(state)

    graph.add_node("plan", plan_node)
    graph.add_node("review", review_node)
    graph.add_node("synth", synth_node)

    graph.set_entry_point("plan")
    graph.add_edge("plan", "review")
    graph.add_edge("review", "synth")

    return graph.compile()
```

### 5. Planner（生成任务流）

```python
import anthropic

client = anthropic.Anthropic()

class PlannerAgent:

    def create_tasks(self, state):
        prompt = f"""
Break this into parallel tasks:

State:
{state}

Return JSON list of tasks.
"""
        res = client.messages.create(
            model="claude-3-5-sonnet-latest",
            max_tokens=1200,
            messages=[{"role": "user", "content": prompt}]
        )
        return res.content[0].text
```

### 6. Reviewer（质量控制）

```python
class ReviewerAgent:

    def review(self, state):
        # 可以接 LLM 或规则系统
        return {
            "status": "ok",
            "score": 0.85,
            "issues": []
        }
```

### 7. Synthesizer（结果合并）

```python
class SynthesizerAgent:

    def merge(self, state):
        return {
            "final_output": "merged result",
            "state": state
        }
```

### 8. 主入口（8 小时运行）

```python
import asyncio
from worker_pool import WorkerPool
from graph.workflow import build_graph

async def main():
    graph = build_graph()
    pool = WorkerPool(size=20)  # 10~50 workers
    await asyncio.gather(pool.start())

if __name__ == "__main__":
    asyncio.run(main())
```

---

## 系统能力

### ✔ 并行能力
- 10~50 Claude Workers
- 同时处理任务队列

### ✔ 可编排能力
- LangGraph DAG workflow
- planner → review → synth

### ✔ 可扩展能力
- 可接 webhook
- 可接 memory DB
- 可接 vector retrieval

### ✔ 稳定性
- Redis queue 解耦
- worker crash 不影响整体

---

## 运行 8 小时可以自动完成

- 📌 产品设计
- 📌 代码生成
- 📌 数据分析
- 📌 research pipeline

### 自动演化

- task 自生成
- workflow 自扩展

---

## 与 v0.3.2 的关系

v0.3.2 定义了 **State Graph AI OS** 的架构：
- State is everything
- Graph controls execution, not AI
- Hermes never decides, only enhances

v0.3.3 是 v0.3.2 的**生产级实现**：
- 引入 Redis Queue 实现任务持久化
- 引入 Worker Pool 实现并行执行
- 引入 LangGraph 实现 DAG 编排
- 支持长时间运行（8h+）

---

**文档版本**: v0.3.3  
**创建日期**: 2026-04-14  
**状态**: 待实现 → 实现中 → 已完成
